{
  "name": "celsiusfahrenheit",
  "version": "1.0.0",
  "description": "Conversor de temperatura Celsius ↔ Fahrenheit",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "eslint src/ __tests__/ scripts/",
    "lint:fix": "eslint src/ __tests__/ scripts/ --fix",
    "ai-review": "node scripts/ai-review.js",
    "terraform:plan": "cd terraform && terraform plan"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "jest": "^30.2.0"
  }
}
